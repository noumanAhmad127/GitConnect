<!DOCTYPE html>
<html>
  <head>
    <title>GitConnect</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <% if notice %>
      <p class="notice text-black w-full py-5 px-3 bg-slate-300"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert text-red-600 w-full py-5 px-3 bg-red-400"><%= alert %></p>
    <% end %>
    <main class="container mx-auto my-14 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
