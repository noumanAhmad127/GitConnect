// Update the like button and like count without reloading the page
console.log("AJAX request received for like");
$("#like-btn-<%= @post.id %>").html("<%= j(render 'likes/like_button', post: @post) %>");
$("#like-count-<%= @post.id %>").text("<%= pluralize(@post.likes.count, 'Like') %>");
